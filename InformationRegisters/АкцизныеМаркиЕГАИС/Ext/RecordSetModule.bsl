
&перед("ПриЗаписи")
Процедура КочетовПриЗаписи(Отказ, Замещение)
	Для каждого стр из ЭтотОбъект цикл		
		АкцизнаяМарка=стр.АкцизнаяМарка.ПолучитьОбъект();
		Если не ЗначениеЗаполнено(стр.АкцизнаяМарка.Номенклатура) и ЗначениеЗаполнено(стр.Основание) тогда
			строкаОснования=стр.Основание.товары.найти(стр.АлкогольнаяПродукция);
			АкцизнаяМарка.Номенклатура=строкаОснования.номенклатура;
			АкцизнаяМарка.Записать();
		КонецЕсли;	
	КонецЦикла;
КонецПроцедуры
